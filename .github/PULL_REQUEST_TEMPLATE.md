## What changed

<!-- One or two sentences. Link an issue if there is one: "Fixes #123". -->

## Why

<!-- The motivation, if it isn't obvious from the diff. -->

## How was this tested?

- [ ] `tests/run_tests.sh` passes locally
- [ ] Added/updated a test under `tests/checks/` for this change
- [ ] Live-tested against real hardware (describe briefly: what target, what
      was verified)

## Checklist

- [ ] If this changes a script's configuration schema or anything the web UI
      reads, `scripts/lab_schema` and the web UI are updated in this same PR
- [ ] Docs (`README.md`, or the relevant `README.*.md` translation) updated
      if user-facing behavior changed
- [ ] No new third-party Python dependency (or it was discussed in an issue
      first)
