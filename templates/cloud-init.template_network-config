version: 2
renderer: ${network_renderer}
ethernets:
  id0:
    match:
      macaddress: "${mymac}"
    dhcp4: false
    addresses:
      - ${myip}/${mymask}
    routes:
      - to: 0.0.0.0/0
        via: ${mygw}
    nameservers:
      search: [${mydomain}]
      addresses: [${mydns}]

